'use strict';
// SCAFFOLD — throws NotImplementedError if any export is called.
// Critical: when implemented, must include numeric tests for leverage math
// (notional, liquidationPrice, liquidationBufferPct ≥ 2.0).
const { guard } = require('../_not-implemented');
module.exports = guard('strategies/perps-sizing.js');
